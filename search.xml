<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>国庆模拟赛后总结</title>
    <url>/2021/10/17/%E5%9B%BD%E5%BA%86%E6%A8%A1%E6%8B%9F%E8%B5%9B%E5%90%8E%E6%80%BB%E7%BB%93/</url>
    <content><![CDATA[<h1 id="赛后总结">赛后总结</h1>
<p><strong>注: 没写完, 因为有的题还没过.</strong></p>
<h2 id="前言">前言</h2>
<p>这个文档不一定在写精妙的算法, 有的部分甚至不是在分析满分解法, 更多的是怎么得<strong>该得的</strong>分以及怎么减少不必要的失分.</p>
<p>即: 目标是在j组复赛中分数最大化.</p>
<h2 id="section">2021/10/01</h2>
<h3 id="t1">T1</h3>
<p>在调错上浪费太多时间了, 最后才发现算法没有问题, 是<strong>switch语句</strong>的问题!!!</p>
<p>教训: 在写代码之前推思路的时候一定要明确它的<strong>正确性</strong>! 发现运行的结果不一样时<strong>不要轻易改代码, 更不要轻易改算法!</strong></p>
<p>调试小技巧: 输出一些东西 or 注释掉大段代码, 这些方法主要是快速定位出错的地方的, 实在不行可以下断点调试(用gdb可以的, 注意加debug symbol). 在怀疑算法出问题的时候在纸上推, <strong>不要动代码</strong>!!!</p>
<p>这题的正解是模运算, 可以快速定位到循环运行多次后的指令, 不必一次次模拟了.</p>
<p>(考场上写的是直接模拟, 拿指针一个一个走的那种, 拿了60分已经很不错了)</p>
<p>正解:</p>
<figure class="highlight c++"><table><tbody><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> dx[<span class="number">4</span>] = {<span class="number">1</span>, <span class="number">0</span>, <span class="number">-1</span>, <span class="number">0</span>};</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> dy[<span class="number">4</span>] = {<span class="number">0</span>, <span class="number">-1</span>, <span class="number">0</span>, <span class="number">1</span>};</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">char</span> cmd[<span class="number">4</span>] = {<span class="string">'E'</span>, <span class="string">'S'</span>, <span class="string">'W'</span>, <span class="string">'N'</span>};</span><br><span class="line"></span><br><span class="line">string ops;</span><br><span class="line"><span class="keyword">int</span> t, currx, curry, cnt[<span class="number">4</span>];</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">inline</span> <span class="keyword">void</span> <span class="title">go</span><span class="params">(<span class="keyword">int</span> op)</span> </span>{</span><br><span class="line">    currx += dx[op]; curry += dy[op];</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>{</span><br><span class="line">    cin &gt;&gt; ops &gt;&gt; t;</span><br><span class="line">    <span class="comment">//先运行一遍命令串</span></span><br><span class="line">    <span class="keyword">int</span> oplen = ops.<span class="built_in">length</span>();</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; oplen; i ++) {</span><br><span class="line">        <span class="keyword">char</span> ch = ops[i];</span><br><span class="line">        <span class="comment">//不知道为什么switch会出问题, 比赛就是在这里卡了半小时</span></span><br><span class="line">        <span class="keyword">if</span>(ch == <span class="string">'E'</span>) <span class="built_in">go</span>(<span class="number">0</span>);</span><br><span class="line">        <span class="keyword">if</span>(ch == <span class="string">'S'</span>) <span class="built_in">go</span>(<span class="number">1</span>);</span><br><span class="line">        <span class="keyword">if</span>(ch == <span class="string">'W'</span>) <span class="built_in">go</span>(<span class="number">2</span>);</span><br><span class="line">        <span class="keyword">if</span>(ch == <span class="string">'N'</span>) <span class="built_in">go</span>(<span class="number">3</span>);</span><br><span class="line">    }</span><br><span class="line"></span><br><span class="line">    <span class="keyword">int</span> cishu = t / oplen;  <span class="comment">//每个操作执行cishu遍, 但还可能有多余</span></span><br><span class="line">    <span class="keyword">int</span> pos = t % oplen;    <span class="comment">//最后一个操作的位置</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">//判断条件</span></span><br><span class="line">    <span class="keyword">if</span>(cishu == <span class="number">0</span>) <span class="keyword">goto</span> OUTPUT; <span class="comment">//指令数 &gt;= t --&gt; 已经走完了, 直接输出就行</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">//第二种情况, 即len &lt; t, 要循环多次</span></span><br><span class="line">    currx *= cishu; curry *= cishu;  <span class="comment">//走一次走到了当前的(x, y), 走cishu次就意味着每个点都走了cishu次, 对结果的影响求积即可</span></span><br><span class="line">    <span class="comment">//好现在还剩几步, 走完它</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; pos; i ++) {</span><br><span class="line">        <span class="keyword">char</span> ch = ops[i];</span><br><span class="line">        <span class="keyword">if</span>(ch == <span class="string">'E'</span>) <span class="built_in">go</span>(<span class="number">0</span>);</span><br><span class="line">        <span class="keyword">if</span>(ch == <span class="string">'S'</span>) <span class="built_in">go</span>(<span class="number">1</span>);</span><br><span class="line">        <span class="keyword">if</span>(ch == <span class="string">'W'</span>) <span class="built_in">go</span>(<span class="number">2</span>);</span><br><span class="line">        <span class="keyword">if</span>(ch == <span class="string">'N'</span>) <span class="built_in">go</span>(<span class="number">3</span>);</span><br><span class="line">    }</span><br><span class="line"></span><br><span class="line">OUTPUT: cout &lt;&lt; currx &lt;&lt; <span class="string">' '</span> &lt;&lt; curry &lt;&lt; endl;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">}</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<h3 id="t2">T2</h3>
<p>在推模拟分解质因数时浪费太多时间了, 这种考"板子"的数学题要在平时多做. 考场上要减少<strong>推板子</strong>的时间.</p>
<p>另外还好这题没打表, "只要是数学题就打表"终究是一种"骗分"的心态. 还是要多推式子.</p>
<p>附: 计算一个数素因数个数的代码</p>
<figure class="highlight c++"><table><tbody><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">fac</span><span class="params">(<span class="keyword">int</span> x)</span> </span>{</span><br><span class="line">    <span class="keyword">int</span> res = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">2</span>; i &lt;= x; i ++) {</span><br><span class="line">        <span class="keyword">if</span>(x % i == <span class="number">0</span>) {</span><br><span class="line">            res ++;</span><br><span class="line">            x /= i;</span><br><span class="line">            i --;	<span class="comment">//这行是尝试再拿这个数除一遍, 因为一个数可能有多个相同的质因子.</span></span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">return</span> res;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>还有就是第十个点<strong>具体的</strong>数据给我们了, 是一个很大的数据, 这个O(N*sqrt(N))的算法显然要算很久(在我电脑上算了<strong>一个半小时</strong>), 所以只能用特判. 考试的时候可以优先用暴力或正解算着这种数据, 得到特判就行.</p>
<p>注: 这道是我两天模拟赛中唯一觉得做得还不错, 没啥太大遗憾的题.</p>
<p>注2: 我的电脑算了<strong>一个半小时</strong>, 终于算出最后一个点的特判是<code>564714</code>!!! 但是为什么第八个点还没过??? 最后是90分.</p>
<h3 id="t3">T3</h3>
<p>模拟题, 字太多考试的时候懒得看了. 之后这种题要多想想! 至少把样例算算, 知道模拟的过程.</p>
<p><del>现在赛后订正的时候也懒得看</del></p>
<h3 id="t4">T4</h3>
<p>四联通问题, 把上下左右当成相邻的节点用bfs访问</p>
<h2 id="section-1">2021/10/03</h2>
<h3 id="t1-1">T1</h3>
<p>一定要注意看清<strong>数据范围</strong>! 就比如这道题说了 <span class="math display">\[
N &lt; 1,000,000
\]</span> 就一定要开1e6, <strong>宁肯开大一点也不要开小</strong>!!! 一位数组不会因为开得跟题中一样大而MLE!!!</p>
<p>教训: <strong>1e5的代码52分, 1e6就ac了.</strong></p>
<p>真的是很可惜的, 明明满分算法想出来了, 思路就是正确的, 甚至代码写得都没问题. 就是因为数组没开大, 失分惨重.</p>
<p>思路再说一下吧, 前缀和. 这种<strong>区间询问或操作一位数组问题的算法</strong>在我目前的知识范围内大多只会是<strong>前缀和, 差分, 树状数组, 线段树</strong>了, 更高级的感觉j组考不到, 现在也不是学这种东西的时候.</p>
<blockquote>
<p>虽然1000万个数似乎很多, 但是, 如果这些数是8字节的整数, 那么将占用大致<strong>80MB</strong>.</p>
<p><em>Introduction to Algorithms(3rd edition)</em></p>
</blockquote>
<h3 id="t2-1">T2</h3>
<figure class="highlight c++"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> N = <span class="number">1e6</span>;</span><br><span class="line"><span class="keyword">int</span> array[N];	<span class="comment">//大约占用4MB空间</span></span><br></pre></td></tr></tbody></table></figure>
<p>这道题因为开了1e8的数组, 用了memset导致<strong>全部的点MLE</strong>.</p>
<p><strong>数组不要开太大!!! (1e8个int约等于380MB)</strong></p>
<p>本来可以得60分暴力分, 结果这题因为这个爆零了.</p>
<p>本题的正解是<strong>离线询问</strong>, 并记录操作, 类似于<strong>化简</strong>到最后一个操作, 就不必模拟了.</p>
<p>赛后还以为是用了几个玄学优化导致的问题.</p>
<p>说到玄学优化, 在这里记录几个好用的玄学优化:(好像扯得有点远了)</p>
<h4 id="cin优化">cin优化</h4>
<figure class="highlight c++"><table><tbody><tr><td class="code"><pre><span class="line">ios_base::<span class="built_in">sync_with_stdio</span>(<span class="literal">false</span>);</span><br><span class="line">cin.<span class="built_in">tie</span>(<span class="number">0</span>);	<span class="comment">//cin.tie(nullptr)在C++11及以上可用</span></span><br></pre></td></tr></tbody></table></figure>
<p>(这两行的意思是关闭与stdio的同步, 以提升cin, cout的速度. 在IO比较多的时候很好用. <strong>注意:用了这个优化就不能用stdio内的函数如<code>printf()</code>和<code>scanf()</code></strong>. 当然printf和scanf的速度是更快的, 如果会写快读快写那更快, <code>putchar()</code>和<code>getchar()</code>的速度不是吹的. )</p>
<h4 id="寄存器操作">寄存器操作</h4>
<figure class="highlight c++"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">for</span>(<span class="keyword">register</span> <span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i ++)</span><br></pre></td></tr></tbody></table></figure>
<p>直接操作寄存器, 这样更快.</p>
<p>p.s. csp用不了内联汇编, 所以不要作死, 这个用用就行了</p>
<h4 id="行内函数inline">行内函数(inline)</h4>
<figure class="highlight c++"><table><tbody><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">inline</span> <span class="keyword">int</span> <span class="title">add</span><span class="params">(<span class="keyword">int</span> x, <span class="keyword">int</span> y)</span> </span>{</span><br><span class="line">    <span class="keyword">return</span> x + y;</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>{</span><br><span class="line">    <span class="built_in">add</span>(<span class="number">1</span>, <span class="number">1</span>);	<span class="comment">//直接就地展开函数, 没有调用关系</span></span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">    相当于</span></span><br><span class="line"><span class="comment">    inline int add(int x, int y) {</span></span><br><span class="line"><span class="comment">    	return x + y;</span></span><br><span class="line"><span class="comment">	}</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>适用于代码少, 调用频繁的函数.</p>
<p>函数调用时会将参数, 基址这些东西压栈. 最后call. 运行结束后会压入返回地址等信息(这些东西快忘了, 不对请指正). 这样调用一次无疑浪费了很多时间精力.(有空的话贴一下反汇编代码看得更清楚)</p>
<p>用<code>inline</code>定义会在调用处就地展开, 个人觉得比较像<code>#define</code>(?)</p>
<p>但是这么用会让代码编译后变得更大, 实际开发中要慎用. (算法比赛中没关系)</p>
<h3 id="t3-1">T3</h3>
<p>这里发现了两轮游戏中的单调性质, 但是竟然在纠结<strong>用哪个数据结构实现</strong>!!!</p>
<p>最后时间<strong>全浪费了</strong>, 暴力都没写完.</p>
<p>以后算法较为简单的题, 一定要先写暴力或者基本思路, <strong>不要先默板子或纠结该默哪个板子!!!</strong></p>
<h3 id="t4-1">T4</h3>
<p>这一题要采用类似图论存图的方法存能开哪里的灯, 然后<strong>bfs</strong>.</p>
<p>题干的意思其实是要找到房间之间的 <strong>"连接"</strong> 关系. 想到这里就能知道要用类似邻接表的方式存图了.</p>
<p>考场上我主要的问题在于只想着打暴力就用dfs, 就能拿分. 但是这个dfs在本机都肉眼可见地慢, 就放弃治疗, 也不想剪枝了.</p>
<p>想到bfs而不是用其它方法个人理解是因为本题要先开相邻的灯, 如果dfs需要走完一个方向才能走另一个方向, 不剪枝极其浪费.</p>
<h2 id="section-2">2021/10/05</h2>
<h3 id="t1-2">T1</h3>
<p>模拟赛中第一次ac.</p>
<p>老师给的题解: 数组模拟 (可以理解为手写双端队列)</p>
<p>自己的解法: <strong>stl大法好</strong>(使用<code>std::deque</code>, 即双端队列, 可以理解为可操作两端的<code>std::queue</code>)</p>
<p>可能会出现的问题: 一些stl内的函数可能性能不如手写的, 例如<code>std::string</code>里面的<code>S.find(P)</code>就是暴力匹配的, 性能不如手写KMP.</p>
<p>这是个重大的时刻, 贴个代码纪念一下, 顺便复习一下deque的用法.</p>
<p>(另外做这题的时候好像<strong>cin忘加了</strong>, 导致浪费了亿点时间调错)</p>
<figure class="highlight c++"><table><tbody><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;deque&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line">deque&lt;<span class="keyword">int</span>&gt; q;   <span class="comment">//需要从前后操作, 双端队列</span></span><br><span class="line"><span class="keyword">int</span> n, id;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">inline</span> <span class="keyword">void</span> <span class="title">clearq</span><span class="params">()</span> </span>{</span><br><span class="line">	<span class="keyword">while</span>(q.<span class="built_in">size</span>()) {</span><br><span class="line">		q.<span class="built_in">pop_front</span>();</span><br><span class="line">	}</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">inline</span> <span class="keyword">void</span> <span class="title">add</span><span class="params">(<span class="keyword">char</span> op, <span class="keyword">int</span> x)</span> </span>{</span><br><span class="line">    <span class="comment">//op == 'L'   左边入队</span></span><br><span class="line">    <span class="comment">//op == 'R'   右边入队</span></span><br><span class="line">    <span class="comment">//cout &lt;&lt; "add()" &lt;&lt; op &lt;&lt; ' ' &lt;&lt; x;</span></span><br><span class="line">    <span class="keyword">if</span>(op == <span class="string">'L'</span>) {</span><br><span class="line">        q.<span class="built_in">push_front</span>(x);</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">if</span>(op == <span class="string">'R'</span>) {</span><br><span class="line">        q.<span class="built_in">push_back</span>(x);</span><br><span class="line">    }</span><br><span class="line"></span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">inline</span> <span class="keyword">void</span> <span class="title">kill_people</span><span class="params">(<span class="keyword">char</span> op, <span class="keyword">int</span> x)</span> </span>{</span><br><span class="line">    <span class="keyword">if</span>(op == <span class="string">'L'</span>) {</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; x; i ++) q.<span class="built_in">pop_front</span>();</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">if</span>(op == <span class="string">'R'</span>) {</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; x; i ++) q.<span class="built_in">pop_back</span>();</span><br><span class="line">    }</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">inline</span> <span class="keyword">int</span> <span class="title">popq</span><span class="params">()</span> </span>{</span><br><span class="line">    <span class="keyword">int</span> res;</span><br><span class="line">    res = q.<span class="built_in">front</span>();</span><br><span class="line">    q.<span class="built_in">pop_front</span>();</span><br><span class="line">    <span class="keyword">return</span> res;</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>{</span><br><span class="line">	<span class="comment">//clearq();</span></span><br><span class="line">	ios_base::<span class="built_in">sync_with_stdio</span>(<span class="literal">false</span>); cin.<span class="built_in">tie</span>(<span class="number">0</span>);</span><br><span class="line">    cin &gt;&gt; n;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= n; i ++) {</span><br><span class="line">    	<span class="comment">//puts("LOOP");</span></span><br><span class="line">        <span class="keyword">char</span> op, op1;</span><br><span class="line">        <span class="keyword">int</span> k;</span><br><span class="line">        cin &gt;&gt; op &gt;&gt; op1;</span><br><span class="line">        <span class="comment">//cout &lt;&lt; op &lt;&lt; ' ' &lt;&lt; op1;</span></span><br><span class="line">        <span class="keyword">if</span>(op == <span class="string">'A'</span>) {</span><br><span class="line">            <span class="built_in">add</span>(op1, ++ id); 	<span class="comment">//只有入队才会有新的编号, 单独记录</span></span><br><span class="line">            <span class="comment">//puts("added!!!")</span></span><br><span class="line">        } <span class="keyword">else</span> {</span><br><span class="line">            cin &gt;&gt; k;</span><br><span class="line">            <span class="built_in">kill_people</span>(op1, k);</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span>(q.<span class="built_in">size</span>()) {</span><br><span class="line">    	cout &lt;&lt; q.<span class="built_in">front</span>() &lt;&lt; endl;	<span class="comment">//从队首开始pop</span></span><br><span class="line">    	q.<span class="built_in">pop_front</span>();</span><br><span class="line">    }</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">}</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<h3 id="t2-2">T2</h3>
<h3 id="t3-2">T3</h3>
<p>暴力解法是每个数跟它的左边和上面比较, 统计出符合条件的, 计算最大的面积.</p>
<p>正解是dp.</p>
<p>预处理<code>column[i][j</code>]和<code>can[i][j][k]</code>(用<code>can[i][j][k]</code>表示从第 i 行到第 j 行中的第 k 列能否接道第 i 行到第 j 行的 k-1 列中): <span class="math display">\[
column[i][j]={column[i-1][j]+1(a[i][j]&gt;=a[i-
1][j]),column[i][j]=1}
\]</span> 其中 <span class="math display">\[
can[i][j][k]=(can[i][j-1][k])\and(column[j][k]&gt;=j-i+1)\and(a[j][k]&gt;=a[j][k-
1])\and(coulumn[j][k-1]&gt;=j-i+1
\]</span></p>
<p>贴一个70分解法(用的不是dp, 也不是我写的)</p>
<figure class="highlight c++"><table><tbody><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;bits/stdc++.h&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="comment">//DP?!不过我不想用正常的方法 那就写点玄学的吧。</span></span><br><span class="line"><span class="keyword">int</span> n,m,ma[<span class="number">205</span>][<span class="number">205</span>],row[<span class="number">205</span>][<span class="number">205</span>],col[<span class="number">205</span>][<span class="number">205</span>];</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span>{</span><br><span class="line">	cin&gt;&gt;n&gt;&gt;m;</span><br><span class="line">	<span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">1</span>;i&lt;=n;++i)</span><br><span class="line">		<span class="keyword">for</span>(<span class="keyword">int</span> j=<span class="number">1</span>;j&lt;=m;++j)cin&gt;&gt;ma[i][j];</span><br><span class="line">	<span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">1</span>;i&lt;=n;++i)</span><br><span class="line">		<span class="keyword">for</span>(<span class="keyword">int</span> j=<span class="number">1</span>;j&lt;=m;++j){</span><br><span class="line">			row[i][j]=<span class="number">1</span>;</span><br><span class="line">			<span class="keyword">for</span>(<span class="keyword">int</span> k=j+<span class="number">1</span>;k&lt;=m;++k){</span><br><span class="line">				<span class="keyword">if</span>(ma[i][k<span class="number">-1</span>]&lt;=ma[i][k])row[i][j]++;</span><br><span class="line">				<span class="keyword">else</span> <span class="keyword">break</span>;</span><br><span class="line">			}</span><br><span class="line">		}</span><br><span class="line">	<span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">1</span>;i&lt;=m;++i)</span><br><span class="line">		<span class="keyword">for</span>(<span class="keyword">int</span> j=<span class="number">1</span>;j&lt;=n;++j){</span><br><span class="line">			col[j][i]=<span class="number">1</span>;</span><br><span class="line">			<span class="keyword">for</span>(<span class="keyword">int</span> k=j+<span class="number">1</span>;k&lt;=n;++k){</span><br><span class="line">				<span class="keyword">if</span>(ma[k<span class="number">-1</span>][i]&lt;=ma[k][i])col[j][i]++;</span><br><span class="line">				<span class="keyword">else</span> <span class="keyword">break</span>;</span><br><span class="line">			}</span><br><span class="line">		}</span><br><span class="line">	<span class="comment">//完了完了 玄学方法开始了 </span></span><br><span class="line">	<span class="keyword">int</span> temp=<span class="number">0</span>,ans=<span class="number">0</span>;</span><br><span class="line">	<span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">1</span>;i&lt;=n;++i)</span><br><span class="line">		<span class="keyword">for</span>(<span class="keyword">int</span> j=<span class="number">1</span>;j&lt;=m;++j)</span><br><span class="line">			<span class="keyword">for</span>(<span class="keyword">int</span> k=<span class="number">1</span>;k&lt;=row[i][j];++k){<span class="comment">//k就是一种长度 </span></span><br><span class="line">				<span class="keyword">int</span> len=*<span class="built_in">min_element</span>(col[i]+j,col[i]+j+k);</span><br><span class="line">				temp=k*len;</span><br><span class="line">				ans=<span class="built_in">max</span>(temp,ans);</span><br><span class="line">			}</span><br><span class="line">	cout&lt;&lt;ans&lt;&lt;endl;</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">} </span><br></pre></td></tr></tbody></table></figure>
<h3 id="t4-2">T4</h3>
<p>这题不知道为什么写的算法没问题, 比赛的时候交了是零分(赛后交了一遍完全一样的代码ac了). 就当这次ac了吧, 但是一定要明白为什么会出这种问题, 比赛的时候要是还出就叫天天不灵, 叫地地不应了.</p>
<p>这题想了蛮久的, 主要是证明算法的正确性.</p>
<p>要注意的就是如果这种贪心题最后推出了一个比较简单的式子不要怀疑, 只要每一步都确定是对的式子就没错, 不要在这里过于纠结, 浪费时间.</p>
<p>思路再说一下: 要让ci * t最大, 就要让ci越大的获得更大时间差(这不是废话吗), 即把ci进行从大到小排序, 依次把最大的两个一样的放到最前面和最后面. 难点是模拟这个放在最前和最后的过程.</p>
<p>贴一下考场上的代码, 前面是排序.</p>
<figure class="highlight c++"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">//安排位置</span></span><br><span class="line">    <span class="keyword">int</span> n1 = <span class="number">1</span>, n2 = n;</span><br><span class="line">    <span class="comment">//[1, n]</span></span><br><span class="line">    <span class="comment">//n1:   开始时间偏移量</span></span><br><span class="line">    <span class="comment">//n2:   结束时间偏移量</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= k, n1 &lt;= n2; i ++, n1 ++, n2 --) {    <span class="comment">//两两一对地插入</span></span><br><span class="line">        <span class="comment">//if(c[i] == c[i + 1])</span></span><br><span class="line">            <span class="comment">//cout &lt;&lt; (c[i] == c[i + 1]) &lt;&lt; ' ' &lt;&lt; c[i] &lt;&lt; ' ' &lt;&lt; n1 &lt;&lt; ' ' &lt;&lt; c[i - 1] &lt;&lt; ' ' &lt;&lt; n2 &lt;&lt; endl;</span></span><br><span class="line">            <span class="comment">//把大(c[i])的放在最后 小的(c[i + 1])放在最前 时间差最大</span></span><br><span class="line">            <span class="comment">//第一次不得分 求时间差乘以最大的(即c[i])即可</span></span><br><span class="line">            <span class="comment">//ans += ((n2 - n1) * c[i]  + 0);</span></span><br><span class="line">            ans += (n2 - n1) * c[i];</span><br><span class="line">    }</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
]]></content>
      <tags>
        <tag>OI</tag>
        <tag>模拟赛</tag>
        <tag>赛后总结</tag>
        <tag>题解</tag>
      </tags>
  </entry>
  <entry>
    <title>Hello World</title>
    <url>/2021/10/06/hello-world/</url>
    <content><![CDATA[<p>如题, 这是我的第一篇文章.</p>
]]></content>
  </entry>
  <entry>
    <title>OI比赛中的那些玄学优化</title>
    <url>/2021/10/22/OI%E6%AF%94%E8%B5%9B%E4%B8%AD%E7%9A%84%E9%82%A3%E4%BA%9B%E7%8E%84%E5%AD%A6%E4%BC%98%E5%8C%96/</url>
    <content><![CDATA[<h1 id="前言">前言</h1>
<p>这是我暑假零基础入门C++的时候整理的小技巧. 现在到了CSP-2021赛前, 我在看笔记的时候发现了这个, 就把它发在我的网站上, 希望能帮到即将参加OI的你, <del>也希望能借此给自己增加Rp</del>.</p>
<p>这也是我第一次OI比赛, 希望能考好点.</p>
<h1 id="读入优化">读入优化</h1>
<p><strong>fread快读/快写代码来自oi-wiki</strong></p>
<h2 id="关闭同步推荐">关闭同步(推荐)</h2>
<figure class="highlight c++"><table><tbody><tr><td class="code"><pre><span class="line">ios_base::<span class="built_in">sync_with_stdio</span>(<span class="literal">false</span>);</span><br><span class="line">cin.<span class="built_in">tie</span>(<span class="number">0</span>);</span><br></pre></td></tr></tbody></table></figure>
<p>关闭iostream与stdio中输入输出流的同步. 使<code>std::cin</code>与<code>std::cout</code>接近于<code>scanf()</code>与<code>printf()</code>的速度.</p>
<h3 id="注意事项">注意事项</h3>
<p>这个小优化是用得最多的, cin和cout慢的要命, 在IO特别多的题中要用cin/cout一定要加.</p>
<p><strong>注意: 加了第一行之后将无法使用<code>printf()</code>, <code>scanf()</code>等<code>stdio.h</code>中的函数, 如果作死用了会导致IO流混乱! </strong></p>
<p><strong>注意2: cin/cout写入文件不是即时的, 所以一定要<code>endl</code>刷新缓冲区(或者<code>\n</code>之类的)</strong></p>
<h2 id="printf和scanf推荐"><code>printf()</code>和<code>scanf()</code>(推荐)</h2>
<p>同上, 速度嗖嗖的. 但是不能在关闭同步后使用.</p>
<p>还能格式化字符串, 不香吗?(比如让你保留8位小数, 只需<code>printf("%.8lf", my_variable)</code>即可, cout还需借助<code>iomanip</code>)</p>
<h2 id="快读快写">快读/快写</h2>
<p>很好用, <code>getchar()</code>和<code>putchar()</code>快如闪电.</p>
<figure class="highlight c++"><table><tbody><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">read</span><span class="params">()</span> </span>{</span><br><span class="line">  <span class="keyword">int</span> x = <span class="number">0</span>, w = <span class="number">1</span>;	<span class="comment">//x是数字, w是符号位</span></span><br><span class="line">  <span class="keyword">char</span> ch = <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">while</span> (ch &lt; <span class="string">'0'</span> || ch &gt; <span class="string">'9'</span>) {  <span class="comment">//!isdigit(ch)</span></span><br><span class="line">    <span class="keyword">if</span> (ch == <span class="string">'-'</span>) w = <span class="number">-1</span>;        </span><br><span class="line">    ch = <span class="built_in">getchar</span>();               </span><br><span class="line">  }</span><br><span class="line">  <span class="keyword">while</span> (ch &gt;= <span class="string">'0'</span> &amp;&amp; ch &lt;= <span class="string">'9'</span>) {  <span class="comment">//isdigit(ch)</span></span><br><span class="line">    x = x * <span class="number">10</span> + (ch - <span class="string">'0'</span>); </span><br><span class="line">    ch = <span class="built_in">getchar</span>();</span><br><span class="line">  }</span><br><span class="line">  <span class="keyword">return</span> x * w;</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="comment">//Example:</span></span><br><span class="line"><span class="comment">//int n = read();</span></span><br></pre></td></tr></tbody></table></figure>
<figure class="highlight c++"><table><tbody><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">inline</span> <span class="keyword">void</span> <span class="title">write</span><span class="params">(<span class="keyword">int</span> x)</span> </span>{</span><br><span class="line">  <span class="keyword">static</span> <span class="keyword">int</span> sta[<span class="number">35</span>];</span><br><span class="line">  <span class="keyword">int</span> top = <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">do</span> {</span><br><span class="line">    sta[top++] = x % <span class="number">10</span>, x /= <span class="number">10</span>;</span><br><span class="line">  } <span class="keyword">while</span> (x);</span><br><span class="line">  <span class="keyword">while</span> (top) <span class="built_in">putchar</span>(sta[--top] + <span class="number">48</span>); <span class="comment">//(int)'0'</span></span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="comment">//Example:</span></span><br><span class="line"><span class="comment">//int a = 5;</span></span><br><span class="line"><span class="comment">//write(a);	</span></span><br></pre></td></tr></tbody></table></figure>
<h2 id="fread较为玄学">fread(较为玄学)</h2>
<figure class="highlight c++"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">namespace</span> IO {</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> MAXSIZE = <span class="number">1</span> &lt;&lt; <span class="number">20</span>;</span><br><span class="line"><span class="keyword">char</span> buf[MAXSIZE], *p1, *p2;</span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> gc()                                                               \</span></span><br><span class="line"><span class="meta">  (p1 == p2 &amp;&amp; (p2 = (p1 = buf) + fread(buf, 1, MAXSIZE, stdin), p1 == p2) \</span></span><br><span class="line"><span class="meta">       ? EOF                                                               \</span></span><br><span class="line"><span class="meta">       : *p1++)</span></span><br><span class="line"><span class="function"><span class="keyword">inline</span> <span class="keyword">int</span> <span class="title">rd</span><span class="params">()</span> </span>{</span><br><span class="line">  <span class="keyword">int</span> x = <span class="number">0</span>, f = <span class="number">1</span>;</span><br><span class="line">  <span class="keyword">char</span> c = <span class="built_in">gc</span>();</span><br><span class="line">  <span class="keyword">while</span> (!<span class="built_in">isdigit</span>(c)) {</span><br><span class="line">    <span class="keyword">if</span> (c == <span class="string">'-'</span>) f = <span class="number">-1</span>;</span><br><span class="line">    c = <span class="built_in">gc</span>();</span><br><span class="line">  }</span><br><span class="line">  <span class="keyword">while</span> (<span class="built_in">isdigit</span>(c)) x = x * <span class="number">10</span> + (c ^ <span class="number">48</span>), c = <span class="built_in">gc</span>();</span><br><span class="line">  <span class="keyword">return</span> x * f;</span><br><span class="line">}</span><br><span class="line"><span class="keyword">char</span> pbuf[<span class="number">1</span> &lt;&lt; <span class="number">20</span>], *pp = pbuf;</span><br><span class="line"><span class="function"><span class="keyword">inline</span> <span class="keyword">void</span> <span class="title">push</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> &amp;c)</span> </span>{</span><br><span class="line">  <span class="keyword">if</span> (pp - pbuf == <span class="number">1</span> &lt;&lt; <span class="number">20</span>) <span class="built_in">fwrite</span>(pbuf, <span class="number">1</span>, <span class="number">1</span> &lt;&lt; <span class="number">20</span>, stdout), pp = pbuf;</span><br><span class="line">  *pp++ = c;</span><br><span class="line">}</span><br><span class="line"><span class="function"><span class="keyword">inline</span> <span class="keyword">void</span> <span class="title">write</span><span class="params">(<span class="keyword">int</span> x)</span> </span>{</span><br><span class="line">  <span class="keyword">static</span> <span class="keyword">int</span> sta[<span class="number">35</span>];</span><br><span class="line">  <span class="keyword">int</span> top = <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">do</span> {</span><br><span class="line">    sta[top++] = x % <span class="number">10</span>, x /= <span class="number">10</span>;</span><br><span class="line">  } <span class="keyword">while</span> (x);</span><br><span class="line">  <span class="keyword">while</span> (top) <span class="built_in">push</span>(sta[--top] + <span class="string">'0'</span>);</span><br><span class="line">}</span><br><span class="line">}  <span class="comment">// namespace IO</span></span><br></pre></td></tr></tbody></table></figure>
<p>如上是基于<code>fread</code>实现的快读快写, 本质是利用文件的缓冲区.</p>
<p>实现时需要将<code>getchar()</code>稍微改改, (代码中<code>\</code>表示显示不下, 写的时候从这一行接着写就行, 无需换行)</p>
<h3 id="注意事项-1">注意事项</h3>
<p>敲这个板子太费时间了, 还是用上面那个基于<code>putchar()</code>和<code>getchar()</code>的那个性价比最高.</p>
<p><strong>好像这个东西很玄学, 一些情况下会使整个程序变慢(?)</strong></p>
<h1 id="执行优化">执行优化</h1>
<h2 id="i较为推荐"><code>++i</code>(较为推荐)</h2>
<p>有研究表明<code>++i</code>比<code>i++</code>快, 说是<code>i++</code>用了一个临时变量.</p>
<p><strong>注: 很多人在网上科普两者的关系, 但是只是看反编译代码, 看到<code>++i</code>比<code>i++</code>用的汇编指令少一两个, 就说<code>++i</code>更快(甚至每一句指令的意思都说一下, 即到底慢在哪了)??? 就算真的是这样这种不懂装懂的态度也不是很好</strong></p>
<p><code>++i</code>执行完后的值跟i是一样的, 可以拿来给其它变量赋值.</p>
<figure class="highlight c++"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> k = ++i;</span><br></pre></td></tr></tbody></table></figure>
<h2 id="register已经死了"><code>register</code>(已经死了)</h2>
<p><code>register</code>, 即直接操作寄存器. 在C++14逐渐被弃用.</p>
<p>在之前的C++版本中都可以放在循环里面提升性能.</p>
<figure class="highlight c++"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">for</span>(<span class="keyword">register</span> <span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; ++i)</span><br></pre></td></tr></tbody></table></figure>
<h2 id="inline内联函数推荐"><code>inline</code>内联函数(推荐)</h2>
<p><code>inline</code>可以在调用处就地展开函数.</p>
<figure class="highlight c++"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">inline</span> <span class="keyword">int</span> <span class="title">lowbit</span><span class="params">(<span class="keyword">int</span> x)</span> </span>{</span><br><span class="line">	<span class="keyword">return</span> x &amp; (-x);</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>{</span><br><span class="line">	<span class="keyword">int</span> x = <span class="number">114514</span>;</span><br><span class="line">	<span class="keyword">int</span> y = <span class="built_in">lowbit</span>(x);</span><br><span class="line">	<span class="comment">//相当于直接运行函数, 没有调用关系</span></span><br><span class="line">	<span class="comment">//inline int lowbit(int x) {</span></span><br><span class="line">	<span class="comment">//		return x &amp; (-x);</span></span><br><span class="line">	<span class="comment">//}</span></span><br><span class="line">    cout &lt;&lt; y &lt;&lt; endl;</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>对于一些需要调用多次的函数, 可以使用<code>inline</code>声明他是内联的.</p>
<p>对于inline函数, 调用时不像普通函数那样, 将地址, 参数等关键信息压入栈(一种内存布局)中, 再执行<code>call</code>指令. 返回时再操作栈, 最后<code>ret</code>. 这样不仅费时间, 还浪费栈空间.</p>
<p>inline函数会在预处理阶段像宏(如<code>#define</code>)在调用处原地展开.</p>
<h3 id="注意事项-2">注意事项</h3>
<ul>
<li>对递归函数没用(有人说递归程序用inline会爆re, 本人亲测不会, 应该是编译器优化掉了)</li>
<li>节省了栈空间和调用开销, 但是增加了程序编译后的体积. 在比赛中没啥事, 但在实际开发中慎用.</li>
</ul>
<h3 id="小实验-探究inline函数的性质诚意满满-真的不看看吗">小实验: 探究inline函数的性质(诚意满满, 真的不看看吗?)</h3>
<p>反编译工具: <code>IDA Pro 7.5 (32-bit)</code></p>
<p>测试环境: <code>Windows 10 x64</code></p>
<p>我们写出如下测试代码:</p>
<figure class="highlight c++"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">//test.cpp</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">inline</span> <span class="keyword">int</span> <span class="title">my_inline_func</span><span class="params">(<span class="keyword">int</span> x)</span> </span>{</span><br><span class="line">    <span class="comment">//lowbit :)</span></span><br><span class="line">    <span class="keyword">return</span> x &amp; (-x);</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>{</span><br><span class="line">    <span class="keyword">int</span> n = <span class="number">0</span>;</span><br><span class="line">    <span class="built_in">scanf</span>(<span class="string">"%d"</span>, &amp;n);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"%d\n"</span>, <span class="built_in">my_inline_func</span>(n));</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>使用<code>g++ test.cpp -O -o test.exe</code>命令(<code>-O</code>表示开启优化, 即使用内联函数)生成可执行文件.</p>
<p>反编译之后:</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">; main()</span><br><span class="line">.text:00401421 ; int __cdecl main(int argc, const char **argv, const char **envp)</span><br><span class="line">.text:00401421                 public _main</span><br><span class="line">.text:00401421 _main           proc near               ; CODE XREF: sub_4011A0+8E↑p</span><br><span class="line">.text:00401421</span><br><span class="line">.text:00401421 argc            = dword ptr  8</span><br><span class="line">.text:00401421 argv            = dword ptr  0Ch</span><br><span class="line">.text:00401421 envp            = dword ptr  10h</span><br><span class="line">.text:00401421</span><br><span class="line">.text:00401421 ; __unwind {</span><br><span class="line">.text:00401421                 push    ebp</span><br><span class="line">.text:00401422                 mov     ebp, esp</span><br><span class="line">.text:00401424                 and     esp, 0FFFFFFF0h</span><br><span class="line">.text:00401427                 sub     esp, 20h</span><br><span class="line">.text:0040142A                 call    ___main</span><br><span class="line">.text:0040142F                 mov     dword ptr [esp+1Ch], 0</span><br><span class="line">.text:00401437                 lea     eax, [esp+1Ch]</span><br><span class="line">.text:0040143B                 mov     [esp+4], eax</span><br><span class="line">.text:0040143F                 mov     dword ptr [esp], offset Format ; "%d"</span><br><span class="line">.text:00401446                 call    _scanf</span><br><span class="line">							;这里应该就是inline函数展开后的样子了, 可以看到并没有调用的过程</span><br><span class="line">.text:0040144B                 mov     edx, [esp+1Ch]</span><br><span class="line">.text:0040144F                 mov     eax, edx</span><br><span class="line">.text:00401451                 neg     eax</span><br><span class="line">.text:00401453                 and     eax, edx</span><br><span class="line">.text:00401455                 mov     [esp+4], eax</span><br><span class="line">.text:00401459                 mov     dword ptr [esp], offset aD_0 ; "%d\n"</span><br><span class="line">.text:00401460                 call    _printf</span><br><span class="line">.text:00401465                 mov     eax, 0</span><br><span class="line">.text:0040146A                 leave</span><br><span class="line">.text:0040146B                 retn</span><br><span class="line">.text:0040146B ; } // starts at 401421</span><br><span class="line">.text:0040146B _main           endp</span><br></pre></td></tr></tbody></table></figure>
<p>可以知道, inline函数只是就地展开, 并没有调用, 这个"调用的"过程在地址上是连续的, 没有涉及到地址和参数的push/pop操作.</p>
<p>看得还不够清楚? 让我们点一下F5</p>
<p>借助IDA Pro强大的Hex-rays插件, 可以清楚地看到main函数编译后变成了这个样子.</p>
<figure class="highlight c++"><table><tbody><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> __cdecl <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">const</span> <span class="keyword">char</span> **argv, <span class="keyword">const</span> <span class="keyword">char</span> **envp)</span></span></span><br><span class="line"><span class="function"></span>{</span><br><span class="line">  <span class="keyword">int</span> v4; <span class="comment">// [esp+1Ch] [ebp-4h] BYREF</span></span><br><span class="line"></span><br><span class="line">  __main();	<span class="comment">//这个是预处理, 即程序的入口点, 不用管.</span></span><br><span class="line">  v4 = <span class="number">0</span>;</span><br><span class="line">  <span class="built_in">scanf</span>(<span class="string">"%d"</span>, &amp;v4);</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">"%d\n"</span>, v4 &amp; -v4);	<span class="comment">//这就是inline函数的魅力 :)</span></span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>现在, inline函数的作用我们的理解已经很透彻了.</p>
<h1 id="算法优化">算法优化</h1>
<h2 id="相关stl">相关STL</h2>
<p>据说set, map常数比较大.</p>
<p>但是我不会手写.</p>
<p>所以用着吧...</p>
<p>但是<code>std::sort()</code>据说很高级, 快排, 堆排, 插入排序三管齐下, 性能很不错.</p>
<p>基本上只要自己写写归并, 剩下交给sort就好.</p>
<h1 id="不要作死">不要作死</h1>
<p><strong>这是在赛前加的, 意在提醒广大选手不要为了优化而作死, 想一个更好的算法或者打暴力拿个部分分不香吗?</strong></p>
<p>以下内容在NOI系列竞赛中<strong>不能用</strong>!!! 不包括全部, 只包括选手们可能会作死用到的(我猜的).</p>
<p>还是以官方说明为准, 这里只是提醒一下.</p>
<h2 id="pragma如私自开o2-或者开栈">pragma(如私自开<code>O2</code>, 或者开栈)</h2>
<p>官方明令禁止</p>
<p>开栈的话需要在本机修改, (方法自行百度). CSP不限制栈空间.</p>
<h2 id="内联汇编">内联汇编</h2>
<p>官方明令禁止.</p>
<p>内联汇编分两种, 一种是<code>__asm__</code>, 一种是开一个数组把机器码放进去, 再<code>memcpy</code>到可执行的内存区域上</p>
<p><strong>都不要用!!!</strong></p>
<p>尤其是第二种, 你在自己的电脑上玩玩算了, 很可能在这台电脑上能运行, 另一台电脑上就出错. 而且这类程序需要高权限, 可执行的内存区域都不太好往里写数据.</p>
<p>最重要的, <strong>比赛用了会爆零!!!</strong></p>
]]></content>
      <tags>
        <tag>OI</tag>
        <tag>常数优化</tag>
        <tag>笔记</tag>
        <tag>奇技淫巧</tag>
      </tags>
  </entry>
  <entry>
    <title>CSP-J 2021赛后总结</title>
    <url>/2021/11/13/CSP-J-2021%E8%B5%9B%E5%90%8E%E6%80%BB%E7%BB%93/</url>
    <content><![CDATA[<h1 id="solution">Solution</h1>
<h2 id="t1">T1</h2>
<figure class="highlight c++"><table><tbody><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> INF 0x3f3f3f3f</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> int long long</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span> l, r, n;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">inline</span> <span class="keyword">int</span> <span class="title">brute</span><span class="params">(<span class="keyword">int</span> n ,<span class="keyword">int</span> l, <span class="keyword">int</span> r)</span> </span>{</span><br><span class="line">    <span class="comment">//bf: 暴力枚举k \in [l, r], 90 pts</span></span><br><span class="line">    <span class="keyword">int</span> ans = -INF;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = l; i &lt;= r; i ++) {</span><br><span class="line">        ans = <span class="built_in">max</span>(ans, i % n);</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">return</span> ans;</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">inline</span> <span class="keyword">int</span> <span class="title">ac</span><span class="params">(<span class="keyword">int</span> n, <span class="keyword">int</span> l, <span class="keyword">int</span> r)</span> </span>{</span><br><span class="line">    <span class="comment">//先要明白余数是循环出现的</span></span><br><span class="line">    <span class="comment">//L mod n = l, R mod n = r</span></span><br><span class="line">    <span class="comment">//则余数是{l, l + 1, l + 2, ..., l + n - 1, ..., 0, 1, 2, ..., n - 1, ..., r}</span></span><br><span class="line">    <span class="keyword">int</span> ans;</span><br><span class="line">    <span class="keyword">if</span>(l / n == r / n) ans = r % n;     <span class="comment">//[l, r]区间比n小 --&gt; 数越大 余数越大 因为没有到n - 1的</span></span><br><span class="line">    <span class="keyword">else</span> ans = n - <span class="number">1</span>;                   <span class="comment">//l / n == r / n --&gt; [l, r]区间比n大 --&gt; 肯定有能取到n - 1的数, 即最大k.</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> ans;</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">signed</span> <span class="title">main</span><span class="params">()</span> </span>{</span><br><span class="line">    ios_base::<span class="built_in">sync_with_stdio</span>(<span class="literal">false</span>); cin.<span class="built_in">tie</span>(<span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">    cin &gt;&gt; n &gt;&gt; l &gt;&gt; r;</span><br><span class="line">    <span class="comment">//cout &lt;&lt; brute(n, l, r) &lt;&lt; endl;</span></span><br><span class="line">    cout &lt;&lt; <span class="built_in">ac</span>(n, l, r) &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">}</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
<h2 id="t2">T2</h2>
<h2 id="t3">T3</h2>
<h2 id="t4">T4</h2>
]]></content>
      <tags>
        <tag>OI</tag>
        <tag>赛后总结</tag>
        <tag>题解</tag>
      </tags>
  </entry>
  <entry>
    <title>启发式合并</title>
    <url>/2021/11/13/%E5%90%AF%E5%8F%91%E5%BC%8F%E5%90%88%E5%B9%B6/</url>
    <content><![CDATA[<h1 id="梦幻布丁hnoi-2009">梦幻布丁(HNOI-2009)</h1>
<figure class="highlight c++"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">/*启发式合并，</span></span><br><span class="line"><span class="comment">时间复杂度分析：</span></span><br><span class="line"><span class="comment">考虑用贡献法来分析，每次合并后，队列长度一定大于等于原来短的长度的两倍，这样相当于每次合并都会让短的长度扩大一倍以上，最多扩大 logN 次，</span></span><br><span class="line"><span class="comment">所以总复杂度O(NlogN)，每次O(logN)。</span></span><br><span class="line"><span class="comment">对于这道题目，我们先求出原序列的答案，对于每一种颜色都用类似链表的数据结构串起来，并记录下尾节点。每次修改，都根据启发式合并的方法来暴力合并，</span></span><br><span class="line"><span class="comment">然后处理一下此次合并对答案的影响（显然答案是不增的）。</span></span><br><span class="line"><span class="comment">但是如果我们把 1 染成 2 并且 hsh[1]&gt;hsh[2]，那么我们应该把 2 接到 1 的后面。这样会有一个问题：本次修改后这个链的颜色是 1（颜色为 2 的链被删除了），</span></span><br><span class="line"><span class="comment">如果接下来修改颜色 2（显然这是合法的），会使得找不到颜色 2 而只能找到颜色 1 了。所以我们需要使用一个 f 数组，表示当我们要寻找颜色 x 时，实际上需</span></span><br><span class="line"><span class="comment">要寻找颜色为 color[x] 的链。如果遇到上面这种情况就要交换交换 color[x] 和 color[y]*/</span> </span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;bits/stdc++.h&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> maxn=<span class="number">100005</span>,maxm=<span class="number">1000005</span>;</span><br><span class="line"><span class="keyword">int</span> n,m,c[maxn],hsh[maxm],sta[maxm],color[maxm],pos[maxm],nxt[maxn],ans;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">read</span><span class="params">()</span></span>{</span><br><span class="line">	<span class="keyword">int</span> ret=<span class="number">0</span>,f=<span class="number">1</span>;<span class="keyword">char</span> ch=<span class="built_in">getchar</span>();</span><br><span class="line">	<span class="keyword">while</span> (!<span class="built_in">isdigit</span>(ch)){<span class="keyword">if</span> (ch==<span class="string">'-'</span>)f=-f;ch=<span class="built_in">getchar</span>();}</span><br><span class="line">	<span class="keyword">while</span> (<span class="built_in">isdigit</span>(ch)) ret=(ret&lt;&lt;<span class="number">3</span>)+(ret&lt;&lt;<span class="number">1</span>)+ch-<span class="string">'0'</span>,ch=<span class="built_in">getchar</span>();</span><br><span class="line">	<span class="keyword">return</span> ret*f;</span><br><span class="line">}</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">merge</span><span class="params">(<span class="keyword">int</span> x,<span class="keyword">int</span> y)</span> </span>{</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i=pos[x];i;i=nxt[i]) ans-=(c[i<span class="number">-1</span>]==y)+(c[i+<span class="number">1</span>]==y);<span class="comment">//合并时需要注意细节 y x y </span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i=pos[x];i;i=nxt[i]) c[i]=y;<span class="comment">//把原有的x统统变成y </span></span><br><span class="line">    nxt[sta[x]]=pos[y],pos[y]=pos[x],hsh[y]+=hsh[x];<span class="comment">//实质就是再不重新建立关系的同时，依旧利用已有的关联关系。换一个解释就是下次处理颜色y时 </span></span><br><span class="line">    pos[x]=sta[x]=hsh[x]=<span class="number">0</span>;                         <span class="comment">//先把原有的x处理掉(刚刚改变成的y的x)，然后再来解决原有的y，等价于把x接到y的屁股后面</span></span><br><span class="line">	                                                <span class="comment">//最后把x原有的不需要的关联关系干掉 </span></span><br><span class="line">}</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span>{</span><br><span class="line">    n=<span class="built_in">read</span>(),m=<span class="built_in">read</span>();</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i=<span class="number">1</span>;i&lt;=n;i++){</span><br><span class="line">        c[i]=<span class="built_in">read</span>(),color[c[i]]=c[i];</span><br><span class="line">        ans+=c[i]!=c[i<span class="number">-1</span>];	<span class="comment">//统计段数 </span></span><br><span class="line">        <span class="keyword">if</span> (!pos[c[i]]) sta[c[i]]=i;</span><br><span class="line">        hsh[c[i]]++,nxt[i]=pos[c[i]],pos[c[i]]=i;</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">while</span> (m--){</span><br><span class="line">        <span class="keyword">int</span> opt=<span class="built_in">read</span>();</span><br><span class="line">        <span class="keyword">if</span> (opt==<span class="number">2</span>) <span class="built_in">printf</span>(<span class="string">"%d\n"</span>,ans);<span class="keyword">else</span>{</span><br><span class="line">            <span class="keyword">int</span> x=<span class="built_in">read</span>(),y=<span class="built_in">read</span>();</span><br><span class="line">            <span class="keyword">if</span> (x==y) <span class="keyword">continue</span>;</span><br><span class="line">            <span class="keyword">if</span> (hsh[color[x]]&gt;hsh[color[y]]) <span class="built_in">swap</span>(color[x],color[y]);</span><br><span class="line">            <span class="keyword">if</span> (!hsh[color[x]]) <span class="keyword">continue</span>;</span><br><span class="line">            <span class="built_in">merge</span>(color[x],color[y]);</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<figure class="highlight c++"><table><tbody><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> N = <span class="number">1e6</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span> nxt[N], pos[N], raw[N], a[N], last[N], color[N], cnt[N];</span><br><span class="line"><span class="keyword">int</span> n, m, ans;</span><br><span class="line"><span class="comment">//int marge(int, int);</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//看std看不懂, 总结出一个小技巧</span></span><br><span class="line"><span class="comment">//嵌套的数组一个个模拟太难, 可以用一下OOP的思想方便理解</span></span><br><span class="line"><span class="comment">//就比如桶排的时候把cnt[a[i]]理解为a[i].cnt就舒服很多</span></span><br><span class="line"><span class="comment">//阅读这个代码时需要经常这样思考. 例如color[raw[i]]可以理解为raw[i].color, 把包在中括号里边的东西看成object就好</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">inline</span> <span class="keyword">int</span> <span class="title">read</span><span class="params">()</span> </span>{</span><br><span class="line">  <span class="keyword">int</span> x = <span class="number">0</span>, w = <span class="number">1</span>;	<span class="comment">//x是数字, w是符号位</span></span><br><span class="line">  <span class="keyword">char</span> ch = <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">while</span> (ch &lt; <span class="string">'0'</span> || ch &gt; <span class="string">'9'</span>) {  <span class="comment">//!isdigit(ch)</span></span><br><span class="line">    <span class="keyword">if</span> (ch == <span class="string">'-'</span>) w = <span class="number">-1</span>;        </span><br><span class="line">    ch = <span class="built_in">getchar</span>();               </span><br><span class="line">  }</span><br><span class="line">  <span class="keyword">while</span> (ch &gt;= <span class="string">'0'</span> &amp;&amp; ch &lt;= <span class="string">'9'</span>) {  <span class="comment">//isdigit(ch)</span></span><br><span class="line">    x = x * <span class="number">10</span> + (ch - <span class="string">'0'</span>); </span><br><span class="line">    ch = <span class="built_in">getchar</span>();</span><br><span class="line">  }</span><br><span class="line">  <span class="keyword">return</span> x * w;</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="comment">//Example:</span></span><br><span class="line"><span class="comment">//int n = read();</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">inline</span> <span class="keyword">int</span> <span class="title">marge</span><span class="params">(<span class="keyword">int</span> x, <span class="keyword">int</span> y)</span> </span>{</span><br><span class="line">    <span class="comment">//把所有x变成y, 小优化见main函数调用的marge地方</span></span><br><span class="line">    <span class="comment">//下面这几行, 链表的遍历, 比较难懂.</span></span><br><span class="line">    <span class="comment">//注意到答案应该是只减不增的, 因为修改的本质是"合并", "段数"越合并越少. 这也是这题最难想的地方, 不是一个个修改再统计段数, 是用链表存储后再模拟"合并"更新答案.</span></span><br><span class="line">    <span class="comment">//这个加法用得很好, 因为跟前面的一样意味着少了一段 跟后面的不一样意味着有少了一段</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = pos[x]; i; i = nxt[i]) ans -= (raw[i - <span class="number">1</span>] == y) + (raw[i + <span class="number">1</span>] == y);    <span class="comment">//更新答案</span></span><br><span class="line">    <span class="comment">//模拟"合并"的操作</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = pos[x]; i; i = nxt[i]) raw[i] = y;    <span class="comment">//修改x ---&gt; y</span></span><br><span class="line">    nxt[last[x]] = pos[y];  <span class="comment">//更新尾节点, 即每段最后一个</span></span><br><span class="line">    pos[y] = pos[x];    <span class="comment">//更新位置</span></span><br><span class="line">    cnt[y] += cnt[x];   <span class="comment">//加上个数</span></span><br><span class="line">    pos[x] = last[x] = cnt[x] = <span class="number">0</span>;    <span class="comment">//合并完之后x没了</span></span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>{</span><br><span class="line">    <span class="comment">//ios_base::sync_with_stdio(false); cin.tie(0);</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">//cin &gt;&gt; n &gt;&gt; m;</span></span><br><span class="line">    n = <span class="built_in">read</span>(); m = <span class="built_in">read</span>();</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= n; i ++) {</span><br><span class="line">        <span class="comment">//cin &gt;&gt; raw[i];</span></span><br><span class="line">        raw[i] = <span class="built_in">read</span>();</span><br><span class="line">        color[raw[i]] = raw[i]; <span class="comment">//color[x]统计第x个颜色</span></span><br><span class="line">        ans += (raw[i] != raw[i - <span class="number">1</span>]);  <span class="comment">//预处理一下"段数", 这个方法很巧妙, 读入的时候只要跟前面的不一样就表示又有一段</span></span><br><span class="line">        cnt[raw[i]] ++; <span class="comment">//cnt就是桶, 即计数数组</span></span><br><span class="line">        <span class="keyword">if</span>(!pos[i]) last[i] = raw[i];     <span class="comment">//每种布丁最后一个的位置</span></span><br><span class="line">        nxt[i] = pos[raw[i]]; pos[raw[i]] = i;</span><br><span class="line">    }</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span>(m --) {</span><br><span class="line">        <span class="keyword">int</span> op1, op2, op3;</span><br><span class="line">        <span class="comment">//cin &gt;&gt; op1;</span></span><br><span class="line">        op1 = <span class="built_in">read</span>();</span><br><span class="line">        <span class="keyword">if</span>(op1 == <span class="number">1</span>) {</span><br><span class="line">            <span class="comment">//cin &gt;&gt; op2 &gt;&gt; op3;</span></span><br><span class="line">            op2 = <span class="built_in">read</span>(); op3 = <span class="built_in">read</span>();</span><br><span class="line">            <span class="keyword">if</span>(cnt[color[op1]] &gt; cnt[color[op2]]) <span class="built_in">swap</span>(color[op1], color[op2]);     <span class="comment">//小优化, 把x变成y和把y变成x, 之后的答案是不受影响的, 所以改操作次数少的.</span></span><br><span class="line">            <span class="keyword">if</span>(op2 == op3 || !cnt[color[op2]]) <span class="keyword">continue</span>;    <span class="comment">//std考虑得很周全, 舍去修改没有的元素和"修改个寂寞", 防止被卡.</span></span><br><span class="line">            <span class="built_in">marge</span>(op2, op3);</span><br><span class="line">        } <span class="keyword">else</span> {</span><br><span class="line">            cout &lt;&lt; ans &lt;&lt; endl;</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">}</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
]]></content>
      <tags>
        <tag>OI</tag>
        <tag>题解</tag>
        <tag>算法</tag>
      </tags>
  </entry>
  <entry>
    <title>POI2010 - TESIntelligence Test</title>
    <url>/2021/11/21/POI2010-TESIntelligence-Test/</url>
    <content><![CDATA[<figure class="highlight c++"><table><tbody><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;vector&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> N = <span class="number">1e6</span>;</span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> YES puts(<span class="meta-string">"TAK"</span>)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> NO puts(<span class="meta-string">"NIE"</span>);</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span> len, tc;</span><br><span class="line">vector&lt;<span class="keyword">int</span>&gt; nxt[N];</span><br><span class="line"></span><br><span class="line"><span class="comment">//思路 开二维vector存一堆指针 nxt[x][y]就是数字x出现的第y次的位置.</span></span><br><span class="line"><span class="comment">//vector动态开内存 不会爆空间.</span></span><br><span class="line"><span class="comment">//二分去找"数字k出现第x次的位置", 实现"快速跳转".</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">bsearch</span><span class="params">(<span class="keyword">int</span> num, <span class="keyword">int</span> expected_pos)</span> </span>{</span><br><span class="line">    <span class="keyword">int</span> l = <span class="number">0</span>, r = nxt[num].<span class="built_in">size</span>() - <span class="number">1</span>, res = len + <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">while</span>(l &lt;= r) {</span><br><span class="line">        <span class="keyword">int</span> mid = (l + r) &gt;&gt; <span class="number">1</span>;</span><br><span class="line">        <span class="keyword">if</span>(nxt[num][mid] &gt; expected_pos) {</span><br><span class="line">            r = mid - <span class="number">1</span>;</span><br><span class="line">            res = mid;</span><br><span class="line">        } <span class="keyword">else</span> l = mid + <span class="number">1</span>;</span><br><span class="line">    }</span><br><span class="line">    <span class="comment">//return nxt[num][res];</span></span><br><span class="line">    <span class="keyword">return</span> (res == len + <span class="number">1</span> ? res : nxt[num][res]);</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>{</span><br><span class="line">    ios_base::<span class="built_in">sync_with_stdio</span>(<span class="literal">false</span>); cin.<span class="built_in">tie</span>(<span class="number">0</span>);</span><br><span class="line">    cin &gt;&gt; len;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= len; i++) {</span><br><span class="line">        <span class="keyword">int</span> tmp;</span><br><span class="line">        cin &gt;&gt; tmp;</span><br><span class="line">        nxt[tmp].<span class="built_in">push_back</span>(i);</span><br><span class="line">    }</span><br><span class="line"></span><br><span class="line">    cin &gt;&gt; tc;</span><br><span class="line">    <span class="keyword">while</span>(tc--) {</span><br><span class="line">        <span class="keyword">int</span> l, a[N];</span><br><span class="line">        cin &gt;&gt; l;</span><br><span class="line">        <span class="keyword">bool</span> flag = <span class="literal">true</span>; <span class="keyword">int</span> pos = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= l; i++) cin &gt;&gt; a[i];</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= l; i++) {</span><br><span class="line">            pos = <span class="built_in">bsearch</span>(a[i], pos);</span><br><span class="line">            <span class="comment">//fprintf(stderr, "a[%d] = %d,\tpos = %d\n", i, a[i], pos);</span></span><br><span class="line">            <span class="keyword">if</span>(pos &gt; len) { flag = <span class="number">0</span>; <span class="keyword">break</span>; }</span><br><span class="line">        }</span><br><span class="line">        </span><br><span class="line">        flag ? YES : NO;</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">}</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>
]]></content>
      <tags>
        <tag>OI</tag>
        <tag>题解</tag>
      </tags>
  </entry>
  <entry>
    <title>FREE-Figurines</title>
    <url>/2021/12/04/FREE-Figurines/</url>
    <content><![CDATA[<figure class="highlight c++"><table><tbody><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> N = <span class="number">1e6</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">两种操作: 接在后面 / 删除最前面.</span></span><br><span class="line"><span class="comment">最差情况: "重组", 即全部删了再拼起来. 需要2n次操作.</span></span><br><span class="line"><span class="comment">优化: 尾巴上的节点不需要操作. 可以统计有多少个这种节点, 减去节点数 * 2即可.</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span> src[N], dest[N], len[N], n, ans;</span><br><span class="line"><span class="keyword">bool</span> vis[N];</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">inline</span> <span class="keyword">void</span> <span class="title">debug</span><span class="params">()</span> </span>{</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"ans = %d\n"</span>, ans);</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>{</span><br><span class="line">    cin &gt;&gt; n;</span><br><span class="line">    <span class="comment">/*预处理, 统计最坏情况ans*/</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= n; i++) {</span><br><span class="line">        cin &gt;&gt; src[i];</span><br><span class="line">        vis[src[i]] = <span class="literal">true</span>;</span><br><span class="line">        <span class="keyword">if</span>(src[i]) ans++;</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= n; i++) {</span><br><span class="line">        cin &gt;&gt; dest[i];</span><br><span class="line">        vis[dest[i]] = <span class="literal">true</span>;</span><br><span class="line">        <span class="keyword">if</span>(dest[i]) ans++;</span><br><span class="line">    }</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= n; i++) {</span><br><span class="line">        <span class="keyword">if</span>(!vis[i]) {</span><br><span class="line">            <span class="keyword">int</span> tmpi = i;</span><br><span class="line">            <span class="keyword">while</span>(src[tmpi] * dest[tmpi] &amp;&amp; src[tmpi] == dest[tmpi]) {   <span class="comment">//不为自由的 &amp;&amp; 相等就是不用断开的链</span></span><br><span class="line">                ans -= <span class="number">2</span>;</span><br><span class="line">                tmpi = src[tmpi];</span><br><span class="line">            }</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line"></span><br><span class="line">    cout &lt;&lt; ans &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
]]></content>
      <tags>
        <tag>OI</tag>
        <tag>题解</tag>
      </tags>
  </entry>
</search>
